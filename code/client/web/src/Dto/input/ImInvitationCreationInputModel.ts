/**
 * Input model for creating an instant messaging invitation.
 *
 * @param expiresAt The date and time when the invitation expires.
 */
export interface ImInvitationCreationInputModel {
    expiresAt: string;
}
